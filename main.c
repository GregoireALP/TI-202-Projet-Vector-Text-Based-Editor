#include <stdio.h>
#include "fonctions.h"
#include "area.h"

int main() {

    int nb_pixels = 0;

    Point *start = create_point(0, 0);
    Point *end = create_point(5, 5);
    Line *line = create_line(start, end);




    return 0;
}
